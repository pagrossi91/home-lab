# homeassistant/secure-remote-access/.env — copy to .env and fill in.
# See CLAUDE.md. Real values live only in .env (gitignored) and the backup.

DUCKDNS_DOMAIN=*.yourdomain.duckdns.org
DUCKDNS_TOKEN=changeme-duckdns-token
EMAIL=you@example.com
