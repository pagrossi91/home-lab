# network-services/pi-hole/piholeconfig.env — copy to piholeconfig.env and
# fill in. See CLAUDE.md. Real values live only in the gitignored file.

ServerIP=192.168.X.X
WEBPASSWORD=changeme-pihole-web-password
